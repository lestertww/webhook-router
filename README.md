# Webhook Router Frontend

A simple Next.js app for a webhook router SaaS.

- Landing page (`/`)
- Dashboard (`/dashboard`)
- Webhook endpoint (`/api/hook/[routerId]`)

## Development
```bash
npm install
npm run dev
